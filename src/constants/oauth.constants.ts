export enum OAuthStatus {
    PENDING = "pending",
    SUCCEED = "success",
    FAILED = "failed"
}

export enum AccountStatus {
    UNACTIVATED = 0,
    ACTIVATED = 1,
    LOCKED = 2,
    DELETED = 3
}