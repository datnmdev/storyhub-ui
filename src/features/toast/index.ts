import toastAction from "./toast.action"
import toastReducer from "./toast.reducer"
import toastSelector from "./toast.selector"

const toastFeature = {
    toastReducer,
    toastAction,
    toastSelector
}

export default toastFeature